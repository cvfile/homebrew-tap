# cvfile/homebrew-tap

Homebrew tap for the `cv` CLI. Auto-populated by GoReleaser on every
`cv-go/v*` tag in [cvfile/cv](https://github.com/cvfile/cv).

## Install

```sh
brew tap cvfile/tap
brew install cv
```
